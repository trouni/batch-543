// This will replace the simple_form with the one containing the error messages
document.getElementById('new_review').outerHTML =
  "<%=j render partial: 'reviews/form', locals: { restaurant: @restaurant, review: @review } %>"